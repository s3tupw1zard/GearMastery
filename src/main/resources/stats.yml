# Global defaults merge before inherited and local profile rules.
config-version: 2
# ADDITIVE caps the resulting absolute value. MULTIPLICATIVE caps the multiplier factor.
defaults:
  DURABILITY:
    enabled: true
    mode: MULTIPLICATIVE
    per-level: 0.005
    cap: 1.50
